var carName = "Volvo"

function square(x){
    return x*x
}
// this is the coreect statement of  square//
// the first one mistake is that you should put x into parenthese//
// the second one mistake is there is one missing parenthese//
// the third one mistake is we should put the parametre before the function name//
function largest(a,b){
if (a>b){    
return a} 
return b}
function add(number1, number2){
return number1+number2
}
function subtract(number1, number2){
return number1-number2 
}
var operator = '+'
var p = 5
var m = 3
function decide(){
if (operator==='+'){
return add(p,m)}
else return subtract(p,m) }
function divide(a,b){
    a/b
} 
function multiply(a,b){
    a*b
} 
